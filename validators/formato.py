def validar_archivo(ruta_archivo, negocio, usuario, reglas_path):
    print(f"Validando archivo {ruta_archivo} para negocio {negocio} por usuario {usuario}")
    print(f"Reglas usadas: {reglas_path}")
